import React, {useState, useEffect} from 'react';
import { Text, View, StyleSheet, Image, ScrollView } from 'react-native';

export default function Inscription() {
    return (
      <Text>C'est ici qu'il faudra s'inscrire.</Text>
    );
};
  